<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Carica application.properties / application.yml -->
    <property resource="application.properties" />

    <springProperty scope="context" name="springAppName" source="spring.application.name" />

    <!-- Pattern console moderno -->
    <property name="CONSOLE_LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} %5p [%X{traceId:-},%X{spanId:-}] [${springAppName}] %logger{36} - %m%n%throwable" />

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeMdc>true</includeMdc>
            <includeContext>true</includeContext>
            <customFields>{"app":"${springAppName}"}</customFields>
        </encoder>
    </appender>

    <root level="${APP_LOGGING_ROOT_LEVEL:-INFO}">
        <appender-ref ref="${log.output.format:-CONSOLE}" />
    </root>

</configuration>